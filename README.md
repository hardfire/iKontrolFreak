#Control your mac remotely over the network
 - Requirements - node, npm, macbook :D

 - Install all node modules
  `npm install`
 - Start the server
  `npm start`

 - Enjoy!

Have a change, request, anything? feel free to reach via the issues
